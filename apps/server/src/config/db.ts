// Database layer removed - assignment does not require persistence
// All data (orderbooks, trades, system status) is real-time in-memory

export const connectDatabase = async () => {
  // No-op: no database needed
};
